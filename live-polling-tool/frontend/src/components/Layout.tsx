import {Link,useNavigate} from 'react-router-dom';import {BarChart3,LogOut,Plus,Radio} from 'lucide-react';import {useAuth} from '../context/AuthContext';import {ReactNode} from 'react';
export default function Layout({children}:{children:ReactNode}){const {user,logout}=useAuth();const nav=useNavigate();return <div className="app"><header><Link className="brand" to="/"><span className="logo"><Radio size={18}/></span> PulsePoll</Link>{user&&<nav><Link to="/"><BarChart3 size={16}/> Dashboard</Link><Link className="primary small" to="/create"><Plus size={16}/> Create poll</Link><button className="ghost" onClick={()=>{logout();nav('/login')}}><LogOut size={16}/></button></nav>}</header><main>{children}</main></div>}
