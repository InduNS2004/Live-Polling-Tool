import {FormEvent,useState} from 'react';import {useNavigate,useLocation} from 'react-router-dom';import {useAuth} from '../context/AuthContext';
export default function Auth({mode}:{mode:'login'|'signup'}){const nav=useNavigate(),loc=useLocation(),{login,signup}=useAuth();const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setError('');setBusy(true);try{mode==='login'?await login(email,password):await signup(email,password);nav(loc.state?.from||'/')}catch(x:any){setError(x.message)}finally{setBusy(false)}}return <section className="auth"><div className="auth-copy"><div className="eyebrow">REAL-TIME POLLING</div><h1>Ask a question.<br/><em>Watch it move.</em></h1><p>Create a poll in seconds and see every response arrive live—without refreshing.</p></div><form className="card auth-card" onSubmit={submit}><h2>{mode==='login'?'Welcome back':'Create your account'}</h2><p className="muted">{mode==='login'?'Sign in to manage your polls.':'Start building live polls today.'}</p>{error&&<div className="alert">{error}</div>}<label>Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><label>Password<input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="At least 8 characters"/></label><button className="primary" disabled={busy}>{busy?'Working…':mode==='login'?'Sign in':'Sign up'}</button><button type="button" className="link-btn" onClick={()=>nav(mode==='login'?'/signup':'/login')}>{mode==='login'?'Need an account? Sign up':'Already have an account? Sign in'}</button></form></section>}
